name: 'Build'

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Test build
    runs-on: ubuntu-latest
    steps:
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: stable
      - name: Build
        run: |
          bun install
          bun run build
      - name: HACS Action
        uses: "hacs/action@main"
        with:
          category: "plugin"